<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"> <!-- NOTE: Set character encoding -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- NOTE: Make page responsive on mobile -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!-- NOTE: Bootstrap CSS -->
  <title>Verify 2FA</title> <!-- NOTE: Browser tab title -->
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark"> <!-- NOTE: Simple dark navbar -->
    <div class="container">
      <a class="navbar-brand" href="/">Supermarket App</a> <!-- NOTE: Brand link back to home -->
    </div>
  </nav>

  <main class="container py-5"> <!-- NOTE: Main content area with vertical padding -->
    <div class="row justify-content-center">
      <div class="col-12 col-sm-10 col-md-6 col-lg-4">
        <div class="card shadow-sm border-0 rounded-4"> <!-- NOTE: Centered card for 2FA form -->
          <div class="card-body p-4">
            <h1 class="h4 mb-3 fw-bold text-center">Two-Factor Verification</h1> <!-- NOTE: Title -->
            <p class="text-secondary text-center mb-4">
              Enter the 6-digit code from your authenticator app.
            </p> <!-- NOTE: Short instruction -->

            <% if (messages && messages.length) { %>
              <!-- NOTE: Show error messages (e.g. invalid code) -->
              <div class="alert alert-danger">
                <% messages.forEach(function(m){ %>
                  <div><%= m %></div>
                <% }) %>
              </div>
            <% } %>

            <form action="/twofactor" method="POST" novalidate>
              <!-- NOTE: Form posts to /twofactor for server-side verification -->

              <div class="mb-3">
                <label for="token" class="form-label">Verification Code</label>
                <input
                  type="tel"
                  id="token"
                  name="token"
                  class="form-control form-control-lg text-center"
                  maxlength="6"
                  pattern="[0-9]{6}"
                  inputmode="numeric"
                  required>
                <!-- NOTE: 6-digit numeric input, centered, required -->

                <div class="form-text">
                  Sent to <%= email %>
                </div>
                <!-- NOTE: Shows which email this 2FA is associated with -->
              </div>

              <button type="submit" class="btn btn-primary btn-lg w-100">
                Verify
              </button>
              <!-- NOTE: Submit button to verify 2FA token -->
            </form>
          </div>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
