<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"> <!-- Set character encoding -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Responsive on mobile -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!-- Bootstrap CSS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> <!-- Bootstrap JS (with Popper) -->
  <title>Supermarket App</title> <!-- Page title shown on browser tab -->
  <style>
    body {
      min-height: 100vh;
      background: linear-gradient(180deg, #f6fbff 0%, #edf3ff 50%, #f5f7fb 100%);
      font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }
    .navbar { box-shadow: 0 2px 6px rgba(0,0,0,.05); }
    .page-card {
      background: #fff;
      border-radius: 22px;
      box-shadow: 0 20px 45px rgba(15, 23, 42, .12), 0 3px 10px rgba(15, 23, 42, .08);
      padding: 2.5rem;
    }
    .hero-intro {
      background: linear-gradient(135deg, #eef4ff 0%, #fef9ff 50%, #ecf3ff 100%);
      border-radius: 22px;
      padding: 1.5rem 2rem;
      border: 1px solid rgba(99, 123, 255, .15);
      margin-bottom: 1.5rem;
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      align-items: center;
      position: relative;
      overflow: hidden;
    }
    .hero-intro::after {
      content: "";
      position: absolute;
      width: 180px;
      height: 180px;
      background: radial-gradient(circle at top right, rgba(255, 143, 223, .35), transparent 70%);
      top: -80px;
      right: -60px;
      border-radius: 50%;
    }
    .hero-icon {
      width: 56px;
      height: 56px;
      border-radius: 50%;
      background: linear-gradient(135deg, #0d6efd, #54a9ff);
      color: #fff;
      font-size: 1.8rem;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 12px 25px rgba(13, 110, 253, .35);
    }
    .hero-text h2 { margin-bottom: .4rem; font-weight: 700; color: #101828; }
    .hero-text p { margin: 0; color: #475467; }
    .hero-badge {
      background: rgba(99, 123, 255, .12);
      color: #4154ff;
      padding: .25rem .85rem;
      border-radius: 999px;
      font-size: .8rem;
      text-transform: uppercase;
      letter-spacing: .08em;
    }
    .form-section-title {
      font-size: .9rem;
      text-transform: uppercase;
      letter-spacing: .08em;
      color: #64748b;
      margin-top: 1rem;
      margin-bottom: .6rem;
    }
    .btn-pill {
      border-radius: 999px;
      padding: .55rem 1.6rem;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: .4rem;
    }
    .dropzone {
      border: 1.5px dashed #cbd5f5;
      border-radius: 18px;
      padding: 1.5rem;
      text-align: center;
      background: rgba(99,123,255,.04);
      color: #475467;
    }
  </style>
</head>
<body>
  <!-- Admin navbar (inventory tab active) -->
  <%- include('partials/adminNav', { active: 'inventory' }) %>

  <!-- Main Content -->
  <div class="container my-4 form-wrapper" style="max-width: 760px;">
    <div class="page-card">
      <div class="hero-intro">
        <div class="hero-icon" aria-hidden="true">🛍️</div>
        <div class="hero-text">
          <h2>Add New Product</h2>
          <p>Upload product details, set stock and pricing, and keep your catalog fresh.</p>
        </div>
        <span class="hero-badge">Inventory control</span>
      </div>

      <!-- Add Product form: sends POST to /addProduct with file upload -->
      <form action="/addProduct" method="POST" enctype="multipart/form-data">
        <div class="form-section-title">Product Info</div>
        <!-- Product name input -->
        <div class="mb-3">
          <label for="name" class="form-label">Product Name</label>
          <input type="text" class="form-control" id="name" name="name" required>
        </div>

        <!-- Quantity input (integer, min 0) -->
        <div class="mb-3">
          <label for="quantity" class="form-label">Quantity</label>
          <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" required>
        </div>

        <!-- Price input (decimal, min 0, 2 decimal places) -->
        <div class="mb-3">
          <label for="price" class="form-label">Price</label>
          <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" required>
        </div>

        <div class="form-section-title">Media</div>
        <!-- Image file upload (only image types allowed) -->
        <div class="mb-4">
          <label for="image" class="form-label">Product Image</label>
          <input class="form-control" type="file" id="image" name="image" accept="image/*" required>
        </div>

        <!-- Submit button -->
        <div class="d-flex justify-content-end gap-2">
          <a href="/inventory" class="btn btn-outline-secondary btn-pill">
            <span aria-hidden="true">↩</span>
            Cancel
          </a>
          <button type="submit" class="btn btn-primary btn-pill">
            <span aria-hidden="true">➕</span>
            Add Product
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Footer layout styles (for fixed footer partial) -->
  <style>
    :root { --app-footer-height: 68px; } /* Custom CSS variable for footer height */
    html, body { height: 100%; } /* Make page take full height */
    body { min-height: 100vh; padding-bottom: calc(var(--app-footer-height) + 28px); } /* Extra padding so content not hidden behind fixed footer */
    .form-wrapper { padding-bottom: calc(var(--app-footer-height) + 40px); } /* Ensure form sits above fixed footer */

    .app-footer { background-color: #0b2131; color: #e6edf3; } /* Footer background and text color */
    .app-footer a { color: #e6edf3; text-decoration: none; }  /* Footer links style */
    .app-footer a:hover { text-decoration: underline; }       /* Footer links hover effect */

    .app-footer-fixed { position: fixed; left: 0; right: 0; bottom: 0; z-index: 1030; } /* Stick footer to bottom of viewport */
    .app-footer-gradient { height: 4px; background: linear-gradient(90deg, var(--bs-primary, #0d6efd), var(--bs-info, #0dcaf0), var(--bs-success, #198754), var(--bs-warning, #ffc107), var(--bs-danger, #dc3545)); } /* Thin colorful gradient bar above footer */
    .app-footer-inner { font-size: .95rem; } /* Slightly smaller footer text */
  </style>

  <!-- Include shared footer partial, with fixed positioning class -->
  <%- include('partials/footer', { footerClass: 'app-footer-fixed' }) %>
</body>
</html>
