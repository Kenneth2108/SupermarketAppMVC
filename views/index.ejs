<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"> <!-- NOTE: Set encoding -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- NOTE: Required for mobile responsive -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!-- NOTE: Bootstrap CSS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> <!-- NOTE: Bootstrap JS -->
  <title>Supermarket App</title> <!-- NOTE: Browser tab title -->

  <style>
    /* NOTE: Fancy gradient background design */
    body { 
      background: radial-gradient(1200px 500px at 80% -10%, #e9f5ff 0%, rgba(233,245,255,0) 60%),
                 radial-gradient(1000px 500px at -20% 10%, #f4f9f0 0%, rgba(244,249,240,0) 60%), #f8fafc; 
    }

    .navbar { box-shadow: 0 2px 6px rgba(0,0,0,.05); } /* NOTE: Light shadow below navbar */
    .hero { padding: 80px 0; } /* NOTE: Space for hero section */
    .hero-title { font-size: 3rem; font-weight: 700; letter-spacing: .4px; } /* NOTE: Big homepage title */
    .hero-subtitle { color: #5c6770; max-width: 720px; } /* NOTE: Subtitle text styling */
    .hero-card { background: #fff; border-radius: 16px; box-shadow: 0 10px 30px rgba(0,0,0,.06); padding: 32px; } /* NOTE: White box for homepage */
    .feature-card { background: #fff; border-radius: 14px; box-shadow: 0 8px 24px rgba(0,0,0,.05); padding: 22px; height: 100%; } /* NOTE: Feature tiles */
    .feature-emoji { font-size: 28px; } /* NOTE: Emoji icons */
    .section-title { font-size: 1.15rem; text-transform: uppercase; letter-spacing: .06em; color: #6c757d; } /* NOTE: Small section header */
  </style>
</head>
<body>

    <% if (user && user.role === 'admin') { %> 
      <!-- NOTE: If admin logged in, show admin navbar -->
      <%- include('partials/adminNav', { active: 'dashboard' }) %>

    <% } else if (user) { %>
      <!-- NOTE: If normal user logged in, show user navbar -->
      <%- include('partials/userNav', { active: 'shop' }) %>

    <% } else { %>
      <!-- NOTE: If not logged in, show simple navbar -->
      <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container">
          <a class="navbar-brand" href="/">Supermarket App</a> <!-- NOTE: Brand logo -->

          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span> <!-- NOTE: Mobile menu button -->
          </button>

          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav ms-auto"> <!-- NOTE: Align login/register to right -->
              <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
              <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
            </ul>
          </div>
        </div>
      </nav>
    <% } %>

    <!-- HERO SECTION -->
    <div class="container hero">
      <div class="row align-items-center g-4">

        <!-- LEFT SIDE CONTENT -->
        <div class="col-lg-7">
          <div class="hero-card">
            <h1 class="hero-title">Fresh Groceries. Better Prices.</h1> <!-- NOTE: Main headline -->

            <p class="hero-subtitle mt-2">
              Shop everyday essentials and fresh produce. Simple, fast, and secure — whether you’re managing inventory as an admin or filling your cart as a shopper.
            </p> <!-- NOTE: Subheading -->

            <% if (!user) { %>
              <!-- NOTE: If user not logged in, show login buttons -->
              <div class="d-flex gap-2 mt-3">
                <a href="/login" class="btn btn-primary btn-lg">Start Shopping</a>
                <a href="/register" class="btn btn-outline-secondary btn-lg">Create Account</a>
              </div>

            <% } else if (user.role === 'admin') { %>
              <!-- NOTE: Admin quick-access buttons -->
              <div class="d-flex gap-2 mt-3 flex-wrap">
                <a href="/inventory" class="btn btn-primary">Manage Products</a>
                <a href="/admin/users" class="btn btn-outline-primary">Manage Users</a>
                <a href="/admin/users/new" class="btn btn-outline-secondary">Add User</a>
              </div>
              <div class="mt-2 text-muted">Welcome back, <strong><%= user.username %></strong> (admin)</div> <!-- NOTE: Admin greeting -->

            <% } else { %>
              <!-- NOTE: Normal logged-in user quick actions -->
              <div class="d-flex gap-2 mt-3 flex-wrap">
                <a href="/shopping" class="btn btn-primary">Go to Store</a>
                <a href="/cart" class="btn btn-outline-secondary">View Cart</a>
                <a href="/orders" class="btn btn-outline-secondary">View Orders</a>
              </div>
              <div class="mt-2 text-muted">Welcome back, <strong><%= user.username %></strong></div> <!-- NOTE: User greeting -->
            <% } %>
          </div>
        </div>

        <!-- RIGHT SIDE IMAGE -->
        <div class="col-lg-5">
          <div class="text-center">
            <img src="/images/bananas.png" class="img-fluid" style="max-height: 260px;" alt="Groceries">
            <!-- NOTE: Homepage hero image -->
          </div>
        </div>
      </div>

      <!-- FEATURES SECTION -->
      <div class="mt-5">
        <div class="section-title mb-3">Why Shop With Us</div> <!-- NOTE: Header above features -->

        <div class="row g-3">
          <div class="col-md-4">
            <div class="feature-card h-100">
              <div class="feature-emoji">🛒</div> <!-- NOTE: Icon -->
              <h5 class="mt-2 mb-1">Easy Shopping</h5>
              <div class="text-muted small">Browse products and add to cart in seconds.</div>
            </div>
          </div>

          <div class="col-md-4">
            <div class="feature-card h-100">
              <div class="feature-emoji">⚡</div>
              <h5 class="mt-2 mb-1">Fast Checkout</h5>
              <div class="text-muted small">Smooth flows to keep you moving.</div>
            </div>
          </div>

          <div class="col-md-4">
            <div class="feature-card h-100">
              <div class="feature-emoji">🔒</div>
              <h5 class="mt-2 mb-1">Secure Accounts</h5>
              <div class="text-muted small">Your data and sessions are kept safe.</div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- FOOTER STYLING -->
    <style>
      :root { --app-footer-height: 68px; } /* NOTE: Footer height variable */

      html, body { height: 100%; } /* NOTE: Allow footer to align bottom */
      body { 
        min-height: 100vh; 
        padding-bottom: calc(var(--app-footer-height) + 28px); /* NOTE: Prevent content from being blocked by footer */
      }

      .app-footer { background-color: #0b2131; color: #e6edf3; } /* NOTE: Footer color */
      .app-footer a { color: #e6edf3; text-decoration: none; } /* NOTE: Footer links */
      .app-footer a:hover { text-decoration: underline; } 

      .app-footer-fixed { 
        position: fixed; left: 0; right: 0; bottom: 0; z-index: 1030; 
      } /* NOTE: Sticky footer */

      .app-footer-gradient { 
        height: 4px; 
        background: linear-gradient(90deg, var(--bs-primary, #0d6efd), var(--bs-info, #0dcaf0), var(--bs-success, #198754), var(--bs-warning, #ffc107), var(--bs-danger, #dc3545));
        /* NOTE: Color bar above footer */
      }

      .app-footer-inner { font-size: .95rem; } /* NOTE: Footer font size */
    </style>

    <%- include('partials/footer', { footerClass: 'app-footer-fixed' }) %> 
    <!-- NOTE: Attach footer and make it fixed at bottom -->
  </body>
</html>
